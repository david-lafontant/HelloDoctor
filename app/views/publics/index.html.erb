<div class="container">
  <h1 class="lead">Hello Doctor</h1>
  <p class="text-primary">Patients records managing system</p>
  <% if user_signed_in? %>
    <%= link_to 'Logout', destroy_user_session_path, data: {turbo_method: :delete, turbo_confirm: "are you sure" } , class: "btn btn-danger" %>
  <% end %>
</div>
